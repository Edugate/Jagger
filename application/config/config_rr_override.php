<?php if (!defined('BASEPATH')) exit('No direct script access allowed');

/**
 * this is dummy file
 * it's loaded after config.php, config_rr.php, jcache.php, email.php when it's running as container
 * particularly useful if config is loaded from docker compose
 */
$config['dummy'] = true;